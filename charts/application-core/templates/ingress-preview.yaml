{{- if and .Values.rollouts.enabled .Values.ingressPreview.enabled .Values.ingress.enabled -}}
{{- $fullName := include "application-core.fullname" . -}}
{{- $svcPort := .Values.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-preview
  labels:
    {{- include "application-core.labels" . | nindent 4 }}
  annotations:
    {{- include "application-core.annotations" $ | nindent 4 }}
  {{- with .Values.ingressPreview.annotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
    nginx.ingress.kubernetes.io/service-upstream: "true"
spec:
  ingressClassName: {{ .Values.ingressPreview.className }}
  {{- if .Values.ingressPreview.tls }}
  tls:
    {{- range .Values.ingressPreview.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingressPreview.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}-preview
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
